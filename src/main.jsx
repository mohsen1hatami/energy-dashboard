import React from "react";
import ReactDOM from "react-dom/client";
import EnergyManagementDashboard from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EnergyManagementDashboard />
  </React.StrictMode>
);
